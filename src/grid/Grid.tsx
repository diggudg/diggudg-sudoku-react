import React, { Component } from "react";

export default class Grid extends Component {

    render() {
        return (<React.Fragment>
            <div>Grid</div>
        </React.Fragment>);
    }
}